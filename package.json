{
  "name": "teambridge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dashboard": "pnpm --filter @teambridge/dashboard dev --host 127.0.0.1",
    "daemon": "pnpm --filter @teambridge/daemon start",
    "seed": "node scripts/seed-demo.mjs",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
